-The station can be selected in config.py by replacing the parameter "station" to "mitilini", "sigri", or "combined".

-Preprocessing and plotting can be toggled on an off in config.py by setting the parameters "preprocessing" and "processing" respectively to "True" and "False"  
